/* Projection-only watchlists. They never read or store bookmaker odds. */
(() => {
  const actions = document.querySelector('.topbar-actions');
  if (!actions) return;
  const open = document.createElement('button');
  open.type = 'button'; open.className = 'text-button'; open.textContent = 'Threshold board';
  actions.insertBefore(open, actions.querySelector('#btn-open-parlay'));

  const modal = document.createElement('div');
  modal.className = 'modal-backdrop'; modal.hidden = true;
  modal.innerHTML = `<section class="modal-card threshold-board-card" role="dialog" aria-modal="true" aria-labelledby="threshold-board-title"><button class="modal-close" aria-label="Close">×</button><p class="eyebrow">PROJECTION WATCHLIST</p><h2 id="threshold-board-title">Positive-line thresholds</h2><p class="threshold-intro">See the highest Over line and lowest Under line that the loaded projection model rates positive at your assumed price. This is a shortlist, not a live Bet365 odds feed.</p><div class="threshold-fields"><label>Game<select class="threshold-game"></select></label><label>Market<select class="threshold-market"><option value="receiving_yards">Receiving yards</option><option value="rushing_yards">Rushing yards</option><option value="passing_yards">Passing yards</option><option value="anytime_td">Anytime TD</option></select></label><label class="threshold-price-field">Assumed decimal odds<input class="threshold-odds" inputmode="decimal" value="1.86"></label></div><button class="secondary-button threshold-load" type="button">Build watchlist</button><p class="field-help threshold-status"></p><section class="threshold-results" hidden><p class="threshold-reading"></p><div class="threshold-table-wrap"><table><thead></thead><tbody></tbody></table></div><p class="field-help threshold-footer"></p></section></section>`;
  document.body.append(modal);

  const style = document.createElement('style');
  style.textContent = `.threshold-board-card{width:min(900px,calc(100vw - 28px));max-height:calc(100vh - 28px);overflow:auto}.threshold-fields{display:grid;grid-template-columns:1.4fr 1fr .7fr;gap:11px}.threshold-fields label{margin-top:10px!important}.threshold-load{margin-top:16px!important}.threshold-status{min-height:18px}.threshold-status.error{color:#ff8d9b}.threshold-results{margin-top:14px}.threshold-reading{margin:0 0 10px!important;padding:10px;border-left:2px solid #8bc5ff;background:rgba(139,197,255,.06);font-size:11px!important}.threshold-reading b{color:#e6eefc}.threshold-table-wrap{max-height:48vh;overflow:auto;border:1px solid #293652;border-radius:9px}.threshold-table-wrap table{width:100%;min-width:650px;border-collapse:collapse}.threshold-table-wrap th,.threshold-table-wrap td{padding:10px;border-bottom:1px solid #293652;text-align:left;font-size:12px}.threshold-table-wrap th{position:sticky;top:0;background:#182238;color:#98a6bf;font:500 10px var(--mono);letter-spacing:.05em;text-transform:uppercase}.threshold-player strong,.threshold-player small{display:block}.threshold-player small{margin-top:3px;color:var(--muted);font-size:10px}.threshold-line{font:600 13px var(--mono)}.threshold-line.over{color:#75f0b4}.threshold-line.under{color:#8bc5ff}.threshold-line.td{color:#ffd27a}.threshold-evaluate{display:block;margin-top:6px;padding:4px 6px;border:1px solid currentColor;border-radius:5px;background:transparent;color:inherit;font:500 10px var(--mono);white-space:nowrap}.threshold-evaluate:hover{background:rgba(255,255,255,.07)}@media(max-width:620px){.threshold-fields{grid-template-columns:1fr}.threshold-table-wrap{max-height:42vh}}`;
  document.head.append(style);

  const game = modal.querySelector('.threshold-game'), market = modal.querySelector('.threshold-market'), priceField = modal.querySelector('.threshold-price-field'), odds = modal.querySelector('.threshold-odds'), load = modal.querySelector('.threshold-load'), status = modal.querySelector('.threshold-status'), results = modal.querySelector('.threshold-results'), intro = modal.querySelector('.threshold-intro'), reading = modal.querySelector('.threshold-reading'), footer = modal.querySelector('.threshold-footer'), head = modal.querySelector('thead'), body = modal.querySelector('tbody');
  const esc = value => String(value ?? '').replace(/[&<>'"]/g, char => ({ '&': '&amp;', '<': '&lt;', '>': '&gt;', "'": '&#39;', '"': '&quot;' }[char]));
  const isTd = () => market.value === 'anytime_td';
  async function request(url) { const response = await fetch(url); const data = await response.json().catch(() => ({})); if (!response.ok) throw new Error(data.detail || 'Could not build the watchlist.'); return data; }
  function setMode() { const td = isTd(); priceField.hidden = td; intro.textContent = td ? 'See each player’s model scoring chance and the minimum decimal Bet365 Yes price required. This is a shortlist, not a live Bet365 odds feed.' : 'See the highest Over line and lowest Under line that the loaded projection model rates positive at your assumed price. This is a shortlist, not a live Bet365 odds feed.'; load.textContent = td ? 'Build TD watchlist' : 'Build watchlist'; results.hidden = true; status.textContent = ''; status.classList.remove('error'); }
  async function loadGames() { game.innerHTML = '<option>Loading games…</option>'; game.disabled = true; const data = await request('/api/evaluator/browse?sort_market=receiving_yards&limit=1'); game.innerHTML = data.games.length ? data.games.map(item => `<option value="${esc(item.key)}">${esc(item.label)}</option>`).join('') : '<option value="">No game context in the active projections</option>'; game.disabled = !data.games.length; }
  const playerCell = row => `<td class="threshold-player"><strong>${esc(row.player_name)}</strong><small>${esc(row.team)}${row.opponent ? ` vs ${esc(row.opponent)}` : ''} · ${esc(row.position)}</small></td>`;
  const evaluateButton = (row, side, line, oddsValue, label) => `<button type="button" class="threshold-evaluate" data-player="${esc(row.player_name)}" data-team="${esc(row.team)}" data-market="${esc(row.market)}" data-side="${side}" data-line="${line}" data-odds="${oddsValue}">${label}</button>`;
  function renderTwoSided(data) { head.innerHTML = '<tr><th>Player</th><th>Projection</th><th>Max Over</th><th>Min Under</th></tr>'; reading.innerHTML = 'At this assumed price: an Over is positive at or below the <b>Max Over</b> line; an Under is positive at or above the <b>Min Under</b> line. The distribution can be asymmetric, so do not use the projection mean alone as a rule.'; footer.textContent = 'Choose Evaluate beside a threshold to open that player in the normal evaluator. The assumed line and price are prefilled; replace them with the exact live Bet365 quote before calculating.'; body.innerHTML = data.rows.map(row => { const action = (side, line, label) => line === null || line === undefined ? '—' : `${line}${evaluateButton(row, side, line, data.assumed_decimal_odds, label)}`; return `<tr>${playerCell(row)}<td class="threshold-line">${Number(row.projection_mean).toFixed(1)}</td><td class="threshold-line over">${action('over', row.over_max_positive_line, 'Evaluate Over')}</td><td class="threshold-line under">${action('under', row.under_min_positive_line, 'Evaluate Under')}</td></tr>`; }).join('') || '<tr><td colspan="4">No matching projections were found for this game and market.</td></tr>'; }
  function renderAnytimeTd(data) { head.innerHTML = '<tr><th>Player</th><th>TD projection</th><th>Model TD chance</th><th>Minimum Bet365 odds</th></tr>'; reading.innerHTML = 'An Anytime TD projection is an expected touchdown count, not a percentage. <b>Minimum Bet365 odds</b> is the model break-even price: the live Yes price must be higher to be model-positive.'; footer.textContent = 'Choose Evaluate TD to open the normal evaluator with Anytime TD Yes and the 0.5 line already selected. Enter the exact live Bet365 odds before calculating.'; body.innerHTML = data.rows.map(row => `<tr>${playerCell(row)}<td class="threshold-line">${Number(row.projection_mean).toFixed(2)}</td><td class="threshold-line td">${(Number(row.model_probability) * 100).toFixed(1)}%</td><td class="threshold-line td">${Number(row.minimum_decimal_odds).toFixed(2)}${evaluateButton(row, 'yes', 0.5, '', 'Evaluate TD')}</td></tr>`).join('') || '<tr><td colspan="4">No Anytime TD projections were found for this game.</td></tr>'; }
  async function build() { const td = isTd(), price = Number(odds.value); if (!td && (!Number.isFinite(price) || price <= 1)) { status.textContent = 'Enter decimal odds above 1.00, for example 1.86.'; status.classList.add('error'); return; } load.disabled = true; load.textContent = td ? 'Building TD watchlist…' : 'Building watchlist…'; status.classList.remove('error'); status.textContent = 'Using the loaded projections only…'; results.hidden = true; try { const data = await request(`/api/evaluator/threshold-board?game=${encodeURIComponent(game.value)}&market=${encodeURIComponent(market.value)}&odds=${encodeURIComponent(td ? 1.86 : price)}`); if (data.mode === 'anytime_td') renderAnytimeTd(data); else renderTwoSided(data); status.textContent = `${data.rows.length} projected player${data.rows.length === 1 ? '' : 's'} · ${data.notice}`; results.hidden = false; } catch (error) { status.textContent = error.message; status.classList.add('error'); } finally { load.disabled = false; load.textContent = td ? 'Build TD watchlist' : 'Build watchlist'; } }
  open.addEventListener('click', async () => { modal.hidden = false; results.hidden = true; status.textContent = ''; try { await loadGames(); } catch (error) { status.textContent = error.message; status.classList.add('error'); } });
  market.addEventListener('change', setMode); modal.querySelector('.modal-close').addEventListener('click', () => { modal.hidden = true; }); modal.addEventListener('click', event => { if (event.target === modal) modal.hidden = true; }); load.addEventListener('click', build);
  body.addEventListener('click', async event => { const button = event.target.closest('.threshold-evaluate'); if (!button) return; button.disabled = true; try { const data = await request(`/api/evaluator/players?q=${encodeURIComponent(button.dataset.player)}&limit=20`); const player = data.players.find(item => item.player_name.toLowerCase() === button.dataset.player.toLowerCase() && item.team.toUpperCase() === button.dataset.team.toUpperCase() && item.markets.includes(button.dataset.market)); if (!player) throw new Error('This player could not be matched safely in the active projections.'); window.proplensStartEvaluation(player, { market: button.dataset.market, side: button.dataset.side, line: Number(button.dataset.line), odds: button.dataset.odds }); modal.hidden = true; } catch (error) { status.textContent = error.message; status.classList.add('error'); button.disabled = false; } });
})();
